# HAV-DF Deepfake Detector — Requirements
# Install with: pip install -r requirements.txt

# Core deep learning
torch>=2.1.0
torchvision>=0.16.0

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0

# Video / image processing
opencv-python>=4.8.0
Pillow>=10.0.0

# Face detection (choose one — dlib is faster on CPU)
# Option A: dlib (requires cmake: sudo apt-get install cmake)
dlib>=19.24.0
# Option B: MTCNN (fallback if dlib unavailable)
facenet-pytorch>=2.5.3

# ML utilities
scikit-learn>=1.3.0
scipy>=1.11.0
numpy>=1.24.0

# Progress bars
tqdm>=4.65.0

# Optional: experiment tracking
# wandb>=0.15.0
